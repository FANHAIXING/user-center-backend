spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jasper
    username: root
    password: 200403
#  session时效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0